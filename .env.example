# =============================================================
# LTT PLATFORM - ENVIRONMENT VARIABLES (Netlify + Development)
# =============================================================
# انسخ هذا الملف إلى .env للتطوير المحلي
# cp .env.example .env
# =============================================================

# ---- Vite (Frontend) ----
# القيمة الافتراضية "/api" تعمل مع Netlify redirect
VITE_API_URL=/api

# ---- Database (Neon PostgreSQL - Serverless) ----
# ⚠️ لا تضع الرابط هنا مباشرة. استخدم Netlify Environment Variables
# DATABASE_URL=postgresql://user:password@ep-example.aws.neon.tech/neondb?sslmode=require

# ---- Authentication ----
SESSION_SECRET=your-session-secret-at-least-32-characters-long
TOKEN_EXPIRY=24h

# ---- CORS ----
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ---- Security ----
NODE_ENV=development
LOG_LEVEL=info

# ---- Seed Data ----
DEFAULT_PASSWORD=LTT@2024
